Domain models (SQLAlchemy) define entities and persistence schema.

Note: Models currently live in `src/app.py` and are exported via `src/__init__.py`. They can be split into module files under `src/models/` in a later refactor.

